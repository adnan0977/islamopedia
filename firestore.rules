
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }
    function isAdmin() {
      return isSignedIn() && exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid));
    }

    // Public Content: Anyone can read/list, only Admin can write
    match /videos/{videoId} {
      allow read, list: if true;
      allow write: if isAdmin();
    }

    match /channels/{channelId} {
      allow read, list: if true;
      allow write: if isAdmin();
    }

    match /quran_editions/{editionId} {
      allow read, list: if true;
      allow write: if isAdmin();
    }

    match /quran/{pageId} {
      allow read, list: if true;
      allow write: if isAdmin();
    }

    match /quran_metadata/{id} {
      allow read, list: if true;
      allow write: if isAdmin();
    }

    match /speakers/{speakerId} {
      allow read, list: if true;
      allow write: if isAdmin();
    }

    match /quran_surahs/{surahId} {
      allow read, list: if true;
      allow write: if isAdmin();
      
      match /ayats/{ayahId} {
        allow read, list: if true;
      }
    }

    // App Settings: Publicly readable, only Admin can write
    match /settings/app_config {
      allow read: if true;
      allow write: if isAdmin();
    }

    // User-specific data
    match /users/{userId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;
    }

    // Roles management
    match /roles_admin/{adminId} {
      allow read: if isSignedIn();
      allow write: if false; 
    }
  }
}
