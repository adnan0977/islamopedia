
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --- Helper Functions ---
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isAdmin() {
      return isSignedIn() && exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid));
    }

    // --- Collection Rules ---

    // Admin Markers - Restricted read
    match /roles_admin/{userId} {
      allow get: if isOwner(userId);
      allow list, create, update, delete: if false;
    }

    // Public Collections - Read for everyone (get and list), Write for Admin only
    
    match /hadith_books/{bookId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }

    match /hadith_editions/{hadithEditionId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }

    match /hadith_data/{contentId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }

    match /videos/{videoId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }

    match /channels/{channelId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }

    match /speakers/{speakerId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }

    match /quran/{surahId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }

    match /quran_editions/{editionId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }

    match /quran_metadata/{metaId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }

    match /settings/{settingId} {
      allow get, list: if true;
      allow create, update, delete: if isAdmin();
    }
  }
}
