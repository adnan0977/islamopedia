
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --- SYSTEM SECURITY METADATA ---
    // Rules Version: 1.1.2 (Allow public Hadith reports)

    // --- Helper Functions ---
    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid));
    }

    // --- Hadith Library (Public Read, Admin Write) ---
    
    match /hadith_books/{id} {
      allow get, list: if true;
      allow write: if isAdmin();
    }

    match /hadith_editions/{id} {
      allow get, list: if true;
      allow write: if isAdmin();
    }

    match /hadith_index/{id} {
      allow get, list: if true;
      allow write: if isAdmin();
    }

    match /hadith_data/{id} {
      allow get, list: if true;
      allow write: if isAdmin();
    }

    match /hadith_reference/{id} {
      allow get, list: if true;
      allow write: if isAdmin();
    }

    match /hadith_reports/{id} {
      allow create: if true;
      allow read, update, delete: if isAdmin();
    }

    // --- Public Content ---
    
    match /languages/{id} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /videos/{id} {
      allow read: if true;
      // Allow anyone to increment the appViewCount, but only admins to change other fields
      allow update: if isAdmin() || (request.resource.data.diff(resource.data).affectedKeys().hasOnly(['appViewCount']));
      allow create, delete: if isAdmin();
    }
    
    match /channels/{id} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /speakers/{id} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /quran/{id} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /quran_editions/{id} {
      allow read: if true;
      allow write: if isAdmin();
    }
    
    match /quran_metadata/{id} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /settings/{id} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // --- Admin Access Control ---
    match /roles_admin/{userId} {
      allow get: if isSignedIn() && request.auth.uid == userId;
      
      // Bootstrapping: Allow the primary developer to initialize their own admin document.
      allow create: if isSignedIn() 
                    && request.auth.uid == userId 
                    && request.resource.data.email == 'adnan@gmail.com';
      
      allow list, update, delete: if false;
    }
  }
}
