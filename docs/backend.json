
{
  "entities": {
    "Video": {
      "title": "Video",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "thumbnailUrl": { "type": "string" },
        "channelId": { "type": "string" },
        "publishedAt": { "type": "string" },
        "isTrending": { "type": "boolean" },
        "isActive": { "type": "boolean" },
        "appViewCount": { "type": "number" }
      }
    },
    "Language": {
      "title": "Language",
      "description": "ISO 639-1 Language mapping for the platform.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "isoCode": { "type": "string" }
      },
      "required": ["name", "isoCode"]
    },
    "HadithBook": {
      "title": "Hadith Book",
      "description": "A primary collection of Hadith (e.g. Sahih Bukhari).",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "bookName": { "type": "string" },
        "editionCount": { "type": "number" },
        "lastSyncedAt": { "type": "string" }
      }
    },
    "HadithEdition": {
      "title": "Hadith Edition",
      "description": "A specific language version of a Hadith book from the API registry.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "bookId": { "type": "string" },
        "name": { "type": "string" },
        "author": { "type": "string" },
        "language": { "type": "string" },
        "has_sections": { "type": "boolean" },
        "direction": { "type": "string" },
        "source": { "type": "string" },
        "comments": { "type": "string" },
        "link": { "type": "string" },
        "linkmin": { "type": "string" },
        "dataSync": { "type": "string", "enum": ["yes", "no"] }
      }
    },
    "HadithReference": {
      "title": "Hadith Reference",
      "description": "Mapping of internal API references to maintain uniqueness.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "hadithId": { "type": "string" },
        "bookId": { "type": "string" },
        "editionId": { "type": "string" },
        "refBook": { "type": "number" },
        "refHadith": { "type": "number" }
      }
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/videos/{videoId}": {
      "schema": "Video",
      "description": "Cataloged spiritual videos."
    },
    "/languages/{langId}": {
      "schema": "Language",
      "description": "Platform language ISO mappings."
    },
    "/hadith_books/{bookId}": {
      "schema": "HadithBook",
      "description": "Primary Hadith collections."
    },
    "/hadith_editions/{editionId}": {
      "schema": "HadithEdition",
      "description": "Language-specific editions."
    },
    "/hadith_data/{contentId}": {
      "schema": "object",
      "description": "Individual Hadith records."
    },
    "/hadith_index/{indexId}": {
      "schema": "object",
      "description": "Chapter definitions for a Hadith book."
    },
    "/hadith_reference/{refId}": {
      "schema": "HadithReference",
      "description": "Unique mapping of Prophetic references."
    },
    "/roles_admin/{userId}": {
      "schema": "object",
      "description": "Admin marker."
    },
    "/settings/{settingId}": {
      "schema": "object",
      "description": "Global config."
    }
  }
}
